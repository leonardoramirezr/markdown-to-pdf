{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build current file to PDF",
            "type": "shell",
            "command": "uv run convert.py \"${file}\"",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "presentation": {
                "reveal": "never",
                "panel": "shared"
            }
        },
        {
            "label": "Build all files to PDF",
            "type": "shell",
            "command": "uv run convert.py",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": [],
            "presentation": {
                "reveal": "never",
                "panel": "shared"
            }
        }
    ]
}
